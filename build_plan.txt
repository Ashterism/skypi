## V1
- [x] Go/No-Go indicator
- [x] Tonight at a glance (ranges for tonight)
- [x] Hourly breakdown (good/bad hours by RAG)

## V1.1 – Provider / orchestrator refactor
- [x] Move latitude / longitude / timezone to `config.py`
- [x] Convert moon provider to read location + timezone from config
- [x] Convert weather provider to read location from config
- [x] Make providers accept **datetime range (start_dt, end_dt)** instead of calculating their own dates
- [x] Remove provider assumptions about "today" or "tonight"
- [x] Ensure providers return **continuous hourly ranges**
- [x] Create `forecaster` service to define the datetime window
- [x] Confirm moon + weather providers return aligned hourly timestamps
- [x] Tag release `v1.1`

## V1.2 – Unified hourly model
- [ ] Create unified hourly dataclass (values + ratings)
- [ ] Merge weather + moon data into unified hourly objects
- [ ] Refactor evaluator to write ratings onto the hourly objects
- [ ] Ensure timestamps remain aligned across providers
- [ ] Update report builder to use unified hourly objects
- [ ] Tag release `v1.2`

## V1.3 – Hourly values + richer hourly UI
- [ ] Update hourly breakdown to show **value + RAG**
- [ ] Decide display format for hourly values
- [ ] Improve hourly table layout
- [ ] Improve readability for the small screen
- [ ] Format time display (Mon 12 Mar – 19:00)
- [ ] Tag release `v1.3`

## V2 – Multi-day forecast
- [ ] Fetch multi-day hourly forecast (~14 days)
- [ ] Generate moon data for the same timestamp range
- [ ] Merge weather + moon into a single dataset
- [ ] Add forecast caching (~15 minute refresh)
- [ ] Store raw forecast in cache file
- [ ] Add helper to group hours by date
- [ ] Add helper to build night window (19:00–01:00)
- [ ] Build nightly summary objects
- [ ] Implement lookahead for **next three nights**
- [ ] Replace hard-coded preview rows with real data
- [ ] Tag release `v2`

## V2.1 – Next good night
- [ ] Scan upcoming nights within a fortnight
- [ ] Define rule for what counts as a "good" night
- [ ] Return first qualifying night
- [ ] Handle "no good night found" case
- [ ] Display next good night in UI
- [ ] Tag release `v2.1`

## Refactor / architecture
- [ ] Ensure providers only fetch/map raw data
- [ ] Keep evaluator pure logic
- [ ] Keep nightly_report as orchestrator
- [ ] Move datetime helpers to `utils/time.py`
- [ ] Add NightSummary dataclass

## Data / caching
- [ ] Create forecast cache file
- [ ] Store timestamp of last API fetch
- [ ] Reuse cached forecast if recent
- [ ] Add optional debug CSV export

## UI polish
- [ ] Improve hourly table readability
- [ ] Finalise GO / NO-GO hero layout
- [ ] Finalise 3-night preview styling
- [ ] Verify layout on target screen resolution


----
Not planned yet

move check_data (forecaster) to /tests



SkyPi/
├── README.md
├── requirements.txt
├── run.py                 # tiny entrypoint (starts Flask)
│
├── skypi/                 # all Python logic lives here
│   ├── __init__.py
│   ├── config.py
│   ├── models.py
│   ├── evaluator.py
│   ├── services/
│   │   └── nightly_report.py
│   ├── providers/
│   │   ├── weather.py
│   │   └── moon.py
│   └── utils/
│       └── time.py
│
├── web/                   # presentation layer only
│   ├── templates/
│   │   ├── index.html
│   │   └── details.html
│   └── static/
│       └── style.css
│
├── data/                  # cache + local state
│   └── cache/
│       └── latest.json
│
├── setup/                 # kiosk/systemd scripts later
│   ├── skypi.service
│   └── kiosk.sh
│
└── tests/                 # optional


requirements.txt

Python dependencies (Flask, requests, astronomy lib, etc).
Used to recreate the environment on the Pi.

run.py

Starts Flask and wires everything together.
Imports get_nightly_report() and passes the result to templates.
Contains no business logic.

⸻

skypi/ (the engine)

__init__.py

Marks this as a Python package.
Usually empty.

config.py

Holds lat/lon, timezone, window (19–01), thresholds.
Central place for tunable values.

models.py

Defines dataclasses like:
	•	HourlyConditions
	•	NightReport
This is your internal data contract.

evaluator.py

Pure logic.
Takes weather + moon data and returns GOOD / OK / BAD + reasons.

⸻

services/nightly_report.py

Orchestrator.
Builds the time window → fetches weather + moon → runs evaluator → returns NightReport.
This is the single “engine entrypoint”.

⸻

providers/weather.py

Calls Open-Meteo.
Maps raw JSON → list of HourlyConditions.

providers/moon.py

Computes moon altitude + illumination per hour.
Returns moon data in your internal format.

⸻

utils/time.py

Handles timezone + window building (19:00–01:00 logic).
Keeps messy datetime logic out of your core code.

⸻

web/ (the skin)

templates/index.html

Main screen (big GOOD / OK / BAD).
Renders a NightReport.

templates/details.html

Hourly breakdown and reasons.
Pure presentation.

static/style.css

Styling for 480×320 layout.
No logic.

⸻

data/cache/latest.json

Stores last API result + timestamp.
Prevents repeated API calls and survives reboot.

⸻

setup/

skypi.service

Systemd service to auto-start SkyPi on boot.

kiosk.sh

Script to launch Chromium in kiosk mode pointing to localhost.

⸻

tests/

Optional.
Unit tests for evaluator, time logic, etc.

⸻

The mental model (important)
	•	skypi/ = brain
	•	web/ = face
	•	run.py = glue
	•	data/ = memory
	•	setup/ = deployment